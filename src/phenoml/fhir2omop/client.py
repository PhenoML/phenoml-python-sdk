# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from .raw_client import AsyncRawFhir2OmopClient, RawFhir2OmopClient
from .types.create_omop_response import CreateOmopResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class Fhir2OmopClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawFhir2OmopClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawFhir2OmopClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawFhir2OmopClient
        """
        return self._raw_client

    def create(
        self, *, fhir_resources: typing.Dict[str, typing.Any], request_options: typing.Optional[RequestOptions] = None
    ) -> CreateOmopResponse:
        """
        Shapes a FHIR R4 resource or Bundle into OMOP Common Data Model v5.4 rows
        (person, visit_occurrence, condition_occurrence, drug_exposure,
        procedure_occurrence, measurement, observation).

        **This is a structural mapping (`mode: "structural"`).** Rows are
        structurally valid OMOP, but every clinical and source `concept_id` is `0`:
        the vocabulary crosswalk that assigns real OMOP `concept_id`s is a planned
        follow-up. Each `*_source_value` carries the verbatim FHIR coding
        (`system#code`), `*_type_concept_id` is set to `32817` (EHR), and the
        response `report` lists a standard-code *suggestion* for each source coding
        (already-standard, an unchecked normalization suggestion, or unmapped). Do
        not treat the output as analytically resolved OMOP until `concept_id`s are
        populated.

        Medication codes are resolved whether they appear inline
        (`medicationCodeableConcept`) or via a `medicationReference` to a contained,
        relative (`Type/id`), or bundle-entry (`urn:uuid`) `Medication` resource.
        A medication with no usable code, resolvable reference, or display is
        reported under `scan_summary.dropped_resources` rather than emitted as a
        blank row. The bundle must contain at least one Patient resource.

        Parameters
        ----------
        fhir_resources : typing.Dict[str, typing.Any]
            FHIR resources (single resource or Bundle). Must contain at least one
            Patient resource. Resources are mapped to OMOP rows; standalone
            Medication resources are consumed by medication references rather than
            mapped to their own table.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateOmopResponse
            FHIR resources mapped to OMOP CDM v5.4 (structural)

        Examples
        --------
        from phenoml import PhenomlClient

        client = PhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.fhir2omop.create(
            fhir_resources={
                "resourceType": "Bundle",
                "type": "collection",
                "entry": [
                    {
                        "resource": {
                            "resourceType": "Patient",
                            "id": "patient-1",
                            "gender": "female",
                            "birthDate": "1985-07-22",
                        }
                    },
                    {
                        "resource": {
                            "resourceType": "Condition",
                            "id": "condition-1",
                            "subject": {"reference": "Patient/patient-1"},
                            "code": {
                                "coding": [
                                    {
                                        "system": "http://snomed.info/sct",
                                        "code": "44054006",
                                        "display": "Type 2 diabetes mellitus",
                                    }
                                ]
                            },
                            "onsetDateTime": "2024-01-15",
                        }
                    },
                    {
                        "resource": {
                            "resourceType": "MedicationRequest",
                            "id": "medreq-1",
                            "status": "active",
                            "subject": {"reference": "Patient/patient-1"},
                            "medicationReference": {"reference": "#med0"},
                            "authoredOn": "2024-01-16",
                            "contained": [
                                {
                                    "resourceType": "Medication",
                                    "id": "med0",
                                    "code": {
                                        "coding": [
                                            {
                                                "system": "http://www.nlm.nih.gov/research/umls/rxnorm",
                                                "code": "860975",
                                                "display": "metformin hydrochloride 500 MG",
                                            }
                                        ]
                                    },
                                }
                            ],
                        }
                    },
                ],
            },
        )
        """
        _response = self._raw_client.create(fhir_resources=fhir_resources, request_options=request_options)
        return _response.data


class AsyncFhir2OmopClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawFhir2OmopClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawFhir2OmopClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawFhir2OmopClient
        """
        return self._raw_client

    async def create(
        self, *, fhir_resources: typing.Dict[str, typing.Any], request_options: typing.Optional[RequestOptions] = None
    ) -> CreateOmopResponse:
        """
        Shapes a FHIR R4 resource or Bundle into OMOP Common Data Model v5.4 rows
        (person, visit_occurrence, condition_occurrence, drug_exposure,
        procedure_occurrence, measurement, observation).

        **This is a structural mapping (`mode: "structural"`).** Rows are
        structurally valid OMOP, but every clinical and source `concept_id` is `0`:
        the vocabulary crosswalk that assigns real OMOP `concept_id`s is a planned
        follow-up. Each `*_source_value` carries the verbatim FHIR coding
        (`system#code`), `*_type_concept_id` is set to `32817` (EHR), and the
        response `report` lists a standard-code *suggestion* for each source coding
        (already-standard, an unchecked normalization suggestion, or unmapped). Do
        not treat the output as analytically resolved OMOP until `concept_id`s are
        populated.

        Medication codes are resolved whether they appear inline
        (`medicationCodeableConcept`) or via a `medicationReference` to a contained,
        relative (`Type/id`), or bundle-entry (`urn:uuid`) `Medication` resource.
        A medication with no usable code, resolvable reference, or display is
        reported under `scan_summary.dropped_resources` rather than emitted as a
        blank row. The bundle must contain at least one Patient resource.

        Parameters
        ----------
        fhir_resources : typing.Dict[str, typing.Any]
            FHIR resources (single resource or Bundle). Must contain at least one
            Patient resource. Resources are mapped to OMOP rows; standalone
            Medication resources are consumed by medication references rather than
            mapped to their own table.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateOmopResponse
            FHIR resources mapped to OMOP CDM v5.4 (structural)

        Examples
        --------
        import asyncio

        from phenoml import AsyncPhenomlClient

        client = AsyncPhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.fhir2omop.create(
                fhir_resources={
                    "resourceType": "Bundle",
                    "type": "collection",
                    "entry": [
                        {
                            "resource": {
                                "resourceType": "Patient",
                                "id": "patient-1",
                                "gender": "female",
                                "birthDate": "1985-07-22",
                            }
                        },
                        {
                            "resource": {
                                "resourceType": "Condition",
                                "id": "condition-1",
                                "subject": {"reference": "Patient/patient-1"},
                                "code": {
                                    "coding": [
                                        {
                                            "system": "http://snomed.info/sct",
                                            "code": "44054006",
                                            "display": "Type 2 diabetes mellitus",
                                        }
                                    ]
                                },
                                "onsetDateTime": "2024-01-15",
                            }
                        },
                        {
                            "resource": {
                                "resourceType": "MedicationRequest",
                                "id": "medreq-1",
                                "status": "active",
                                "subject": {"reference": "Patient/patient-1"},
                                "medicationReference": {"reference": "#med0"},
                                "authoredOn": "2024-01-16",
                                "contained": [
                                    {
                                        "resourceType": "Medication",
                                        "id": "med0",
                                        "code": {
                                            "coding": [
                                                {
                                                    "system": "http://www.nlm.nih.gov/research/umls/rxnorm",
                                                    "code": "860975",
                                                    "display": "metformin hydrochloride 500 MG",
                                                }
                                            ]
                                        },
                                    }
                                ],
                            }
                        },
                    ],
                },
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(fhir_resources=fhir_resources, request_options=request_options)
        return _response.data
