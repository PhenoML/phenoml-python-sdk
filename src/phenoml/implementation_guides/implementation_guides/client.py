# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ..types.implementation_guide_detail import ImplementationGuideDetail
from ..types.implementation_guide_list_response import ImplementationGuideListResponse
from ..types.implementation_guide_summary import ImplementationGuideSummary
from .raw_client import AsyncRawImplementationGuidesClient, RawImplementationGuidesClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ImplementationGuidesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawImplementationGuidesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawImplementationGuidesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawImplementationGuidesClient
        """
        return self._raw_client

    def list(self, *, request_options: typing.Optional[RequestOptions] = None) -> ImplementationGuideListResponse:
        """
        Returns every implementation guide on this instance — both guides that
        have stored metadata (a profile_context) and guides referenced by at
        least one custom profile — with the number of profiles in each.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImplementationGuideListResponse
            List of implementation guides

        Examples
        --------
        from phenoml import PhenomlClient

        client = PhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.implementation_guides.implementation_guides.list()
        """
        _response = self._raw_client.list(request_options=request_options)
        return _response.data

    def get(self, name: str, *, request_options: typing.Optional[RequestOptions] = None) -> ImplementationGuideDetail:
        """
        Returns a single implementation guide, including its profile_context and
        the ids of the profiles that belong to it.

        Parameters
        ----------
        name : str
            The implementation guide name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImplementationGuideDetail
            The requested implementation guide

        Examples
        --------
        from phenoml import PhenomlClient

        client = PhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.implementation_guides.implementation_guides.get(
            name="acme-cardiology",
        )
        """
        _response = self._raw_client.get(name, request_options=request_options)
        return _response.data

    def update(
        self,
        name: str,
        *,
        profile_context: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ImplementationGuideSummary:
        """
        Sets (or clears, with an empty value) the natural-language profile_context
        for an implementation guide. The context is injected into the LLM during
        resource detection to help select the right profiles from this guide.
        It applies to every profile in the guide.

        Parameters
        ----------
        name : str
            The implementation guide name.

        profile_context : typing.Optional[str]
            Natural-language context that helps the LLM select the right profiles from this implementation guide during resource detection. An empty value clears the context. Max 2000 characters.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImplementationGuideSummary
            Implementation guide successfully updated

        Examples
        --------
        from phenoml import PhenomlClient

        client = PhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.implementation_guides.implementation_guides.update(
            name="acme-cardiology",
        )
        """
        _response = self._raw_client.update(name, profile_context=profile_context, request_options=request_options)
        return _response.data

    def delete(self, name: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Deletes the stored metadata for an implementation guide — its
        profile_context and timestamps. Member profiles keep their
        implementation_guide assignment, so a guide still referenced by at least
        one profile continues to appear in listings, just without context or
        timestamps.

        Parameters
        ----------
        name : str
            The implementation guide name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from phenoml import PhenomlClient

        client = PhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.implementation_guides.implementation_guides.delete(
            name="acme-cardiology",
        )
        """
        _response = self._raw_client.delete(name, request_options=request_options)
        return _response.data


class AsyncImplementationGuidesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawImplementationGuidesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawImplementationGuidesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawImplementationGuidesClient
        """
        return self._raw_client

    async def list(self, *, request_options: typing.Optional[RequestOptions] = None) -> ImplementationGuideListResponse:
        """
        Returns every implementation guide on this instance — both guides that
        have stored metadata (a profile_context) and guides referenced by at
        least one custom profile — with the number of profiles in each.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImplementationGuideListResponse
            List of implementation guides

        Examples
        --------
        import asyncio

        from phenoml import AsyncPhenomlClient

        client = AsyncPhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.implementation_guides.implementation_guides.list()


        asyncio.run(main())
        """
        _response = await self._raw_client.list(request_options=request_options)
        return _response.data

    async def get(
        self, name: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ImplementationGuideDetail:
        """
        Returns a single implementation guide, including its profile_context and
        the ids of the profiles that belong to it.

        Parameters
        ----------
        name : str
            The implementation guide name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImplementationGuideDetail
            The requested implementation guide

        Examples
        --------
        import asyncio

        from phenoml import AsyncPhenomlClient

        client = AsyncPhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.implementation_guides.implementation_guides.get(
                name="acme-cardiology",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(name, request_options=request_options)
        return _response.data

    async def update(
        self,
        name: str,
        *,
        profile_context: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ImplementationGuideSummary:
        """
        Sets (or clears, with an empty value) the natural-language profile_context
        for an implementation guide. The context is injected into the LLM during
        resource detection to help select the right profiles from this guide.
        It applies to every profile in the guide.

        Parameters
        ----------
        name : str
            The implementation guide name.

        profile_context : typing.Optional[str]
            Natural-language context that helps the LLM select the right profiles from this implementation guide during resource detection. An empty value clears the context. Max 2000 characters.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImplementationGuideSummary
            Implementation guide successfully updated

        Examples
        --------
        import asyncio

        from phenoml import AsyncPhenomlClient

        client = AsyncPhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.implementation_guides.implementation_guides.update(
                name="acme-cardiology",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            name, profile_context=profile_context, request_options=request_options
        )
        return _response.data

    async def delete(self, name: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Deletes the stored metadata for an implementation guide — its
        profile_context and timestamps. Member profiles keep their
        implementation_guide assignment, so a guide still referenced by at least
        one profile continues to appear in listings, just without context or
        timestamps.

        Parameters
        ----------
        name : str
            The implementation guide name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from phenoml import AsyncPhenomlClient

        client = AsyncPhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.implementation_guides.implementation_guides.delete(
                name="acme-cardiology",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(name, request_options=request_options)
        return _response.data
