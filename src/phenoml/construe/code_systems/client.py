# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ..types.code_response import CodeResponse
from ..types.delete_code_system_response import DeleteCodeSystemResponse
from ..types.export_code_system_response import ExportCodeSystemResponse
from ..types.get_code_system_detail_response import GetCodeSystemDetailResponse
from ..types.list_code_systems_response import ListCodeSystemsResponse
from .raw_client import AsyncRawCodeSystemsClient, RawCodeSystemsClient
from .types.upload_request_format import UploadRequestFormat
from .types.upload_response import UploadResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class CodeSystemsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawCodeSystemsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawCodeSystemsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawCodeSystemsClient
        """
        return self._raw_client

    def upload(
        self,
        *,
        name: str,
        version: str,
        format: UploadRequestFormat,
        revision: typing.Optional[float] = OMIT,
        file: typing.Optional[str] = OMIT,
        code_col: typing.Optional[str] = OMIT,
        desc_col: typing.Optional[str] = OMIT,
        defn_col: typing.Optional[str] = OMIT,
        codes: typing.Optional[typing.Sequence[CodeResponse]] = OMIT,
        replace: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UploadResponse:
        """
        Upload a custom medical code system with codes and descriptions for use in code extraction. Requires a paid plan.
        Returns 202 immediately; embedding generation runs asynchronously. Poll
        GET /construe/codes/systems/{codesystem}?version={version} to check when status
        transitions from "processing" to "ready" or "failed".

        Parameters
        ----------
        name : str
            Name of the code system. Names are case-insensitive and stored uppercase.
            Builtin system names (e.g. ICD-10-CM, SNOMED_CT_US_LITE, LOINC, CPT, etc.) are
            reserved and cannot be used for custom uploads; attempts return HTTP 403 Forbidden.

        version : str
            Version of the code system

        format : UploadRequestFormat
            Upload format

        revision : typing.Optional[float]
            Optional revision number

        file : typing.Optional[str]
            The file contents as a base64-encoded string.
            For CSV format, this is the CSV file contents.
            For JSON format, this is a base64-encoded JSON array; prefer using 'codes' instead.

        code_col : typing.Optional[str]
            Column name containing codes (required for CSV format)

        desc_col : typing.Optional[str]
            Column name containing descriptions (required for CSV format)

        defn_col : typing.Optional[str]
            Optional column name containing long definitions (for CSV format)

        codes : typing.Optional[typing.Sequence[CodeResponse]]
            The codes to upload as a JSON array (JSON format only).
            This is the preferred way to upload JSON codes, as it avoids unnecessary base64 encoding.
            If both 'codes' and 'file' are provided, 'codes' takes precedence.

        replace : typing.Optional[bool]
            If true, replaces an existing code system with the same name and version.
            Builtin systems cannot be replaced; attempts to do so return HTTP 403 Forbidden.
            When false (default), uploading a duplicate returns 409 Conflict.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UploadResponse
            Upload accepted for asynchronous processing

        Examples
        --------
        from phenoml import PhenomlClient
        from phenoml.construe import CodeResponse

        client = PhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.construe.code_systems.upload(
            name="CUSTOM_CODES",
            version="1.0",
            format="json",
            codes=[
                CodeResponse(
                    code="X001",
                    description="Example custom code 1",
                ),
                CodeResponse(
                    code="X002",
                    description="Example custom code 2",
                ),
            ],
        )
        """
        _response = self._raw_client.upload(
            name=name,
            version=version,
            format=format,
            revision=revision,
            file=file,
            code_col=code_col,
            desc_col=desc_col,
            defn_col=defn_col,
            codes=codes,
            replace=replace,
            request_options=request_options,
        )
        return _response.data

    def list(self, *, request_options: typing.Optional[RequestOptions] = None) -> ListCodeSystemsResponse:
        """
        Returns the terminology server's catalog of available code systems, including both built-in standard terminologies and custom uploaded systems.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListCodeSystemsResponse
            List of available code systems

        Examples
        --------
        from phenoml import PhenomlClient

        client = PhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.construe.code_systems.list()
        """
        _response = self._raw_client.list(request_options=request_options)
        return _response.data

    def find(
        self,
        codesystem: str,
        *,
        version: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetCodeSystemDetailResponse:
        """
        Returns full metadata for a single code system, including timestamps and builtin status.

        Parameters
        ----------
        codesystem : str
            Code system name (e.g., "ICD-10-CM", "SNOMED_CT_US_LITE")

        version : typing.Optional[str]
            Specific version of the code system. Required if multiple versions exist.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetCodeSystemDetailResponse
            Code system detail

        Examples
        --------
        from phenoml import PhenomlClient

        client = PhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.construe.code_systems.find(
            codesystem="ICD-10-CM",
            version="2025",
        )
        """
        _response = self._raw_client.find(codesystem, version=version, request_options=request_options)
        return _response.data

    def delete(
        self,
        codesystem: str,
        *,
        version: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeleteCodeSystemResponse:
        """
        Deletes a custom (non-builtin) code system and all its codes. Builtin systems cannot be deleted.
        Only available on dedicated instances. Large systems may take up to a minute to delete.

        Parameters
        ----------
        codesystem : str
            Code system name

        version : typing.Optional[str]
            Specific version of the code system. Required if multiple versions exist.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteCodeSystemResponse
            Code system deleted successfully

        Examples
        --------
        from phenoml import PhenomlClient

        client = PhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.construe.code_systems.delete(
            codesystem="CUSTOM_CODES",
            version="version",
        )
        """
        _response = self._raw_client.delete(codesystem, version=version, request_options=request_options)
        return _response.data

    def export(
        self,
        codesystem: str,
        *,
        version: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ExportCodeSystemResponse:
        """
        Exports a custom (non-builtin) code system as a JSON file compatible with the upload format.
        The exported file can be re-uploaded directly via POST /construe/upload with format "json".
        Only available on dedicated instances. Builtin systems cannot be exported.

        Parameters
        ----------
        codesystem : str
            Code system name

        version : typing.Optional[str]
            Specific version of the code system. Required if multiple versions exist.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ExportCodeSystemResponse
            Exported code system as JSON file

        Examples
        --------
        from phenoml import PhenomlClient

        client = PhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.construe.code_systems.export(
            codesystem="CUSTOM_CODES",
            version="version",
        )
        """
        _response = self._raw_client.export(codesystem, version=version, request_options=request_options)
        return _response.data


class AsyncCodeSystemsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawCodeSystemsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawCodeSystemsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawCodeSystemsClient
        """
        return self._raw_client

    async def upload(
        self,
        *,
        name: str,
        version: str,
        format: UploadRequestFormat,
        revision: typing.Optional[float] = OMIT,
        file: typing.Optional[str] = OMIT,
        code_col: typing.Optional[str] = OMIT,
        desc_col: typing.Optional[str] = OMIT,
        defn_col: typing.Optional[str] = OMIT,
        codes: typing.Optional[typing.Sequence[CodeResponse]] = OMIT,
        replace: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UploadResponse:
        """
        Upload a custom medical code system with codes and descriptions for use in code extraction. Requires a paid plan.
        Returns 202 immediately; embedding generation runs asynchronously. Poll
        GET /construe/codes/systems/{codesystem}?version={version} to check when status
        transitions from "processing" to "ready" or "failed".

        Parameters
        ----------
        name : str
            Name of the code system. Names are case-insensitive and stored uppercase.
            Builtin system names (e.g. ICD-10-CM, SNOMED_CT_US_LITE, LOINC, CPT, etc.) are
            reserved and cannot be used for custom uploads; attempts return HTTP 403 Forbidden.

        version : str
            Version of the code system

        format : UploadRequestFormat
            Upload format

        revision : typing.Optional[float]
            Optional revision number

        file : typing.Optional[str]
            The file contents as a base64-encoded string.
            For CSV format, this is the CSV file contents.
            For JSON format, this is a base64-encoded JSON array; prefer using 'codes' instead.

        code_col : typing.Optional[str]
            Column name containing codes (required for CSV format)

        desc_col : typing.Optional[str]
            Column name containing descriptions (required for CSV format)

        defn_col : typing.Optional[str]
            Optional column name containing long definitions (for CSV format)

        codes : typing.Optional[typing.Sequence[CodeResponse]]
            The codes to upload as a JSON array (JSON format only).
            This is the preferred way to upload JSON codes, as it avoids unnecessary base64 encoding.
            If both 'codes' and 'file' are provided, 'codes' takes precedence.

        replace : typing.Optional[bool]
            If true, replaces an existing code system with the same name and version.
            Builtin systems cannot be replaced; attempts to do so return HTTP 403 Forbidden.
            When false (default), uploading a duplicate returns 409 Conflict.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UploadResponse
            Upload accepted for asynchronous processing

        Examples
        --------
        import asyncio

        from phenoml import AsyncPhenomlClient
        from phenoml.construe import CodeResponse

        client = AsyncPhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.construe.code_systems.upload(
                name="CUSTOM_CODES",
                version="1.0",
                format="json",
                codes=[
                    CodeResponse(
                        code="X001",
                        description="Example custom code 1",
                    ),
                    CodeResponse(
                        code="X002",
                        description="Example custom code 2",
                    ),
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.upload(
            name=name,
            version=version,
            format=format,
            revision=revision,
            file=file,
            code_col=code_col,
            desc_col=desc_col,
            defn_col=defn_col,
            codes=codes,
            replace=replace,
            request_options=request_options,
        )
        return _response.data

    async def list(self, *, request_options: typing.Optional[RequestOptions] = None) -> ListCodeSystemsResponse:
        """
        Returns the terminology server's catalog of available code systems, including both built-in standard terminologies and custom uploaded systems.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListCodeSystemsResponse
            List of available code systems

        Examples
        --------
        import asyncio

        from phenoml import AsyncPhenomlClient

        client = AsyncPhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.construe.code_systems.list()


        asyncio.run(main())
        """
        _response = await self._raw_client.list(request_options=request_options)
        return _response.data

    async def find(
        self,
        codesystem: str,
        *,
        version: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetCodeSystemDetailResponse:
        """
        Returns full metadata for a single code system, including timestamps and builtin status.

        Parameters
        ----------
        codesystem : str
            Code system name (e.g., "ICD-10-CM", "SNOMED_CT_US_LITE")

        version : typing.Optional[str]
            Specific version of the code system. Required if multiple versions exist.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetCodeSystemDetailResponse
            Code system detail

        Examples
        --------
        import asyncio

        from phenoml import AsyncPhenomlClient

        client = AsyncPhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.construe.code_systems.find(
                codesystem="ICD-10-CM",
                version="2025",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.find(codesystem, version=version, request_options=request_options)
        return _response.data

    async def delete(
        self,
        codesystem: str,
        *,
        version: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeleteCodeSystemResponse:
        """
        Deletes a custom (non-builtin) code system and all its codes. Builtin systems cannot be deleted.
        Only available on dedicated instances. Large systems may take up to a minute to delete.

        Parameters
        ----------
        codesystem : str
            Code system name

        version : typing.Optional[str]
            Specific version of the code system. Required if multiple versions exist.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteCodeSystemResponse
            Code system deleted successfully

        Examples
        --------
        import asyncio

        from phenoml import AsyncPhenomlClient

        client = AsyncPhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.construe.code_systems.delete(
                codesystem="CUSTOM_CODES",
                version="version",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(codesystem, version=version, request_options=request_options)
        return _response.data

    async def export(
        self,
        codesystem: str,
        *,
        version: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ExportCodeSystemResponse:
        """
        Exports a custom (non-builtin) code system as a JSON file compatible with the upload format.
        The exported file can be re-uploaded directly via POST /construe/upload with format "json".
        Only available on dedicated instances. Builtin systems cannot be exported.

        Parameters
        ----------
        codesystem : str
            Code system name

        version : typing.Optional[str]
            Specific version of the code system. Required if multiple versions exist.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ExportCodeSystemResponse
            Exported code system as JSON file

        Examples
        --------
        import asyncio

        from phenoml import AsyncPhenomlClient

        client = AsyncPhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.construe.code_systems.export(
                codesystem="CUSTOM_CODES",
                version="version",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.export(codesystem, version=version, request_options=request_options)
        return _response.data
