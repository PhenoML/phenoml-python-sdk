# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from .raw_client import AsyncRawConstrueClient, RawConstrueClient
from .types.extract_codes_result import ExtractCodesResult
from .types.feedback_response import FeedbackResponse
from .types.list_codes_response import ListCodesResponse

if typing.TYPE_CHECKING:
    from .code_systems.client import AsyncCodeSystemsClient, CodeSystemsClient
    from .codes.client import AsyncCodesClient, CodesClient
# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ConstrueClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawConstrueClient(client_wrapper=client_wrapper)
        self._client_wrapper = client_wrapper
        self._code_systems: typing.Optional[CodeSystemsClient] = None
        self._codes: typing.Optional[CodesClient] = None

    @property
    def with_raw_response(self) -> RawConstrueClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawConstrueClient
        """
        return self._raw_client

    def list(
        self,
        codesystem: str,
        *,
        version: typing.Optional[str] = None,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListCodesResponse:
        """
        Returns a paginated list of all codes in the specified code system from the terminology server.

        Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.

        Parameters
        ----------
        codesystem : str
            Code system name (e.g., "ICD-10-CM", "SNOMED_CT_US_LITE")

        version : typing.Optional[str]
            Specific version of the code system. Required if multiple versions exist.

        cursor : typing.Optional[str]
            Pagination cursor from previous response

        limit : typing.Optional[int]
            Maximum number of codes to return (default 20)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListCodesResponse
            Paginated list of codes

        Examples
        --------
        from phenoml import PhenomlClient

        client = PhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.construe.list(
            codesystem="ICD-10-CM",
            version="2025",
            cursor="cursor",
            limit=1,
        )
        """
        _response = self._raw_client.list(
            codesystem, version=version, cursor=cursor, limit=limit, request_options=request_options
        )
        return _response.data

    def submit_feedback(
        self,
        *,
        text: str,
        received_result: ExtractCodesResult,
        expected_result: ExtractCodesResult,
        detail: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> FeedbackResponse:
        """
        Submits user feedback on results from the Construe extraction endpoint.
        Feedback includes the full extraction result received and the result the user expected.

        Parameters
        ----------
        text : str
            The natural language text that was used for code extraction

        received_result : ExtractCodesResult

        expected_result : ExtractCodesResult

        detail : typing.Optional[str]
            Optional details explaining the feedback

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        FeedbackResponse
            Feedback saved successfully

        Examples
        --------
        from phenoml import PhenomlClient
        from phenoml.construe import (
            ExtractCodesResult,
            ExtractedCodeResult,
            ExtractRequestSystem,
        )

        client = PhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.construe.submit_feedback(
            text="Patient has type 2 diabetes with hyperglycemia",
            received_result=ExtractCodesResult(
                system=ExtractRequestSystem(
                    name="ICD-10-CM",
                    version="2025",
                ),
                codes=[
                    ExtractedCodeResult(
                        code="E11.9",
                        description="Type 2 diabetes mellitus without complications",
                        valid=True,
                    )
                ],
            ),
            expected_result=ExtractCodesResult(
                system=ExtractRequestSystem(
                    name="ICD-10-CM",
                    version="2025",
                ),
                codes=[
                    ExtractedCodeResult(
                        code="E11.65",
                        description="Type 2 diabetes mellitus with hyperglycemia",
                        valid=True,
                    )
                ],
            ),
            detail="Expected code E11.65 because the text mentions hyperglycemia",
        )
        """
        _response = self._raw_client.submit_feedback(
            text=text,
            received_result=received_result,
            expected_result=expected_result,
            detail=detail,
            request_options=request_options,
        )
        return _response.data

    @property
    def code_systems(self):
        if self._code_systems is None:
            from .code_systems.client import CodeSystemsClient  # noqa: E402

            self._code_systems = CodeSystemsClient(client_wrapper=self._client_wrapper)
        return self._code_systems

    @property
    def codes(self):
        if self._codes is None:
            from .codes.client import CodesClient  # noqa: E402

            self._codes = CodesClient(client_wrapper=self._client_wrapper)
        return self._codes


class AsyncConstrueClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawConstrueClient(client_wrapper=client_wrapper)
        self._client_wrapper = client_wrapper
        self._code_systems: typing.Optional[AsyncCodeSystemsClient] = None
        self._codes: typing.Optional[AsyncCodesClient] = None

    @property
    def with_raw_response(self) -> AsyncRawConstrueClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawConstrueClient
        """
        return self._raw_client

    async def list(
        self,
        codesystem: str,
        *,
        version: typing.Optional[str] = None,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListCodesResponse:
        """
        Returns a paginated list of all codes in the specified code system from the terminology server.

        Usage of CPT is subject to AMA requirements: see PhenoML Terms of Service.

        Parameters
        ----------
        codesystem : str
            Code system name (e.g., "ICD-10-CM", "SNOMED_CT_US_LITE")

        version : typing.Optional[str]
            Specific version of the code system. Required if multiple versions exist.

        cursor : typing.Optional[str]
            Pagination cursor from previous response

        limit : typing.Optional[int]
            Maximum number of codes to return (default 20)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListCodesResponse
            Paginated list of codes

        Examples
        --------
        import asyncio

        from phenoml import AsyncPhenomlClient

        client = AsyncPhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.construe.list(
                codesystem="ICD-10-CM",
                version="2025",
                cursor="cursor",
                limit=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            codesystem, version=version, cursor=cursor, limit=limit, request_options=request_options
        )
        return _response.data

    async def submit_feedback(
        self,
        *,
        text: str,
        received_result: ExtractCodesResult,
        expected_result: ExtractCodesResult,
        detail: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> FeedbackResponse:
        """
        Submits user feedback on results from the Construe extraction endpoint.
        Feedback includes the full extraction result received and the result the user expected.

        Parameters
        ----------
        text : str
            The natural language text that was used for code extraction

        received_result : ExtractCodesResult

        expected_result : ExtractCodesResult

        detail : typing.Optional[str]
            Optional details explaining the feedback

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        FeedbackResponse
            Feedback saved successfully

        Examples
        --------
        import asyncio

        from phenoml import AsyncPhenomlClient
        from phenoml.construe import (
            ExtractCodesResult,
            ExtractedCodeResult,
            ExtractRequestSystem,
        )

        client = AsyncPhenomlClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.construe.submit_feedback(
                text="Patient has type 2 diabetes with hyperglycemia",
                received_result=ExtractCodesResult(
                    system=ExtractRequestSystem(
                        name="ICD-10-CM",
                        version="2025",
                    ),
                    codes=[
                        ExtractedCodeResult(
                            code="E11.9",
                            description="Type 2 diabetes mellitus without complications",
                            valid=True,
                        )
                    ],
                ),
                expected_result=ExtractCodesResult(
                    system=ExtractRequestSystem(
                        name="ICD-10-CM",
                        version="2025",
                    ),
                    codes=[
                        ExtractedCodeResult(
                            code="E11.65",
                            description="Type 2 diabetes mellitus with hyperglycemia",
                            valid=True,
                        )
                    ],
                ),
                detail="Expected code E11.65 because the text mentions hyperglycemia",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_feedback(
            text=text,
            received_result=received_result,
            expected_result=expected_result,
            detail=detail,
            request_options=request_options,
        )
        return _response.data

    @property
    def code_systems(self):
        if self._code_systems is None:
            from .code_systems.client import AsyncCodeSystemsClient  # noqa: E402

            self._code_systems = AsyncCodeSystemsClient(client_wrapper=self._client_wrapper)
        return self._code_systems

    @property
    def codes(self):
        if self._codes is None:
            from .codes.client import AsyncCodesClient  # noqa: E402

            self._codes = AsyncCodesClient(client_wrapper=self._client_wrapper)
        return self._codes
